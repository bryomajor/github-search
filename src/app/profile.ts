export class Profile {

    constructor(public name: string, public login: string, public bio: string, public hireable: string, public blog: string, public email: string, public followers: number, public following: number, public public_gists: number, public public_repos: number) {}
}
