export class Profile {

    constructor(public avatar_url: string, public html_url: string, public name: string, public login: string, public bio: string, public hireable: string, public blog: string, public email: string, public followers: number, public following: number, public public_gists: number, public public_repos: number) {}
}
